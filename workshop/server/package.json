{
  "name": "server",
  "version": "1.0.0",
  "description": "server.js contains a simple express.js server that will serve the tar files containing each exercise.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "buffer-equal-constant-time": "^1.0.1",
    "express": "^4.18.2",
    "node-ssh": "^6.0.0",
    "uptimer": "^0.1.2"
  }
}
